[pytest]
# Discover tests from the tests/ directory.
testpaths = tests
# Add the project root to sys.path so `src` package is importable.
pythonpath = .
# Show short summary for all test outcomes.
addopts = -v --tb=short
